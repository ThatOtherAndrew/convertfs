[project]
name = "convertfs"
version = "0.1.0"
description = "File format conversion as a FUSE filesystem"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyfuse3>=3.4.2",
    "pyvips>=3.1.1",
    "trio>=0.33.0",
]
